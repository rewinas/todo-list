'use strict';

module.exports = function(Task) {
    // Task.beforeRemote('create', function(context, user, next) {
    //     context.args.data.date = Date.now();
    //     context.args.ownerId = context.req.accessToken.userId;
    //     next();
    //   });

    
};
